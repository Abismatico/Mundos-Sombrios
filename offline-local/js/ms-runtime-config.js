/* Mundos Sombrios — Runtime OFFLINE LOCAL v2.11.2 */
(function(){
  'use strict';
  window.MS_RUNTIME_CONFIG = {
    environment: 'offline-local',
    offlineMode: true,
    sandboxMode: false,
    storageNamespace: 'ms-local-v2100',
    supabase: { url:'', publishableKey:'', environment:'unlinked' }
  };
})();
