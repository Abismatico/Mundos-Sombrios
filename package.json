{
  "name": "mundos-sombrios",
  "version": "2.8.6",
  "private": true,
  "type": "module",
  "scripts": {
    "syntax": "node tests/syntax-check.mjs",
    "test": "node --test tests/*.test.js",
    "build": "npm run syntax && npm test && node scripts/build-site.mjs"
  }
}
