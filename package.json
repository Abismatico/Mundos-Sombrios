{
  "name": "mundos-sombrios",
  "version": "2.11.2",
  "private": true,
  "type": "module",
  "scripts": {
    "syntax": "node tests/syntax-check.mjs",
    "test": "node --test tests/*.test.js",
    "build": "node scripts/sync-version.mjs && npm run syntax && npm test && node scripts/build-site.mjs"
  }
}
