{
  "name": "seite-mundos-sombrios",
  "version": "2.5.3",
  "private": true,
  "type": "module",
  "description": "Portal e plataforma de RPG Mundos Sombrios",
  "scripts": {
    "check": "node --test tests/*.test.js",
    "syntax": "node tests/syntax-check.mjs",
    "build": "npm run audit && node scripts/build-site.mjs",
    "test": "node --test tests/*.test.js",
    "audit": "npm run syntax && npm run test"
  },
  "engines": {
    "node": ">=20"
  }
}
